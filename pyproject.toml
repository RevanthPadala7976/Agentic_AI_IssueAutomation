[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "github-issue-triage-agent"
version = "0.1.0"
description = "Agentic AI Issue Automation Engine using Hybrid RAG"
authors = [{ name = "Revanth" }]
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]